# 🌌 Dotfiles // Arch Linux & Hyprland

Добро пожаловать в мой репозиторий dotfiles. Здесь хранятся конфигурационные файлы для моего рабочего окружения на базе **Arch Linux** и тайлового оконного менеджера **Hyprland**. 

---



## 🛠 Технологический стек

* **ОС:** Arch Linux
* **WM:** Hyprland (Wayland)
* **Терминал:** Kitty
* **Оболочка:** Fish
* **Редактор:** Vim
* **Мультиплексор:** Tmux
* **Файловый менеджер:** Yazi
* **Панель:** Waybar
* **Лаунчер:** Rofi (Wayland)
* **Уведомления:** Mako

---

## 📂 Структура репозитория

Конфиги организованы таким образом, чтобы их можно было легко линковать в домашнюю директорию с помощью утилиты `GNU Stow`.

### 🔹 `.config/` (Пользовательские настройки)
* `fastfetch/` — Конфигурация вывода системной информации.
* `fish/` — Настройки оболочки, алиасы, функции и промпт.
* `hypr/` — Основные настройки оконного менеджера (бинды, правила окон, автостарт).
* `kitty/` — Конфигурация терминала (шрифты, цвета, горячие клавиши).
* `mako/` — Внешний вид и правила для всплывающих уведомлений.
* `quickshell/` — Настройки для кастомных UI-компонентов на Wayland.
* `rofi/` — Темы и настройки меню запуска приложений.
* `waybar/` — Кастомная статусная панель (модули, стили CSS).
* `yazi/` — Конфигурация быстрого консольного файлового менеджера.

### 🔹 Утилиты в корне (`~/`)
* `.gitconfig` — Глобальные настройки Git (имя, email, алиасы).
* `.ssh/config` — Настройки SSH-подключений и хостов. **(Внимание: приватные ключи добавлены в `.gitignore` и никогда не коммитятся в публичный репозиторий!)**
* `.tmux/` & `.tmux.conf` — Плагины и конфигурация Tmux для удобной работы с сессиями терминала.
* `.vim/` & `.vimrc` — Моя конфигурация Vim, включая менеджер плагинов, цветовые схемы и автозагрузку.

---

## 🚀 Установка

Для автоматического создания символических ссылок из этого репозитория в вашу домашнюю директорию используется `GNU Stow`.

### 1. Клонирование репозитория
Склонируйте этот репозиторий в скрытую папку в вашем домашнем каталоге:
```bash
git clone https://github.com/weaknessssss/.dotfiles.git
cd ~/.dotfiles
```
### 2. Использование GNU Stow

Так как структура папок в репозитории полностью повторяет структуру домашней директории (например, папка .config лежит в корне репозитория), вам достаточно выполнить одну команду из папки `~/.dotfiles:`

```bash
stow .
```
Справка: Команда stow . возьмет все файлы и папки из текущей директории (.config, .vim, .tmux и т.д.) и создаст симлинки на них в директории на уровень выше (то есть в ~/). Если вы попытаетесь сделать stow mako внутри папки .config, симлинки по ошибке окажутся прямо в корне ~/.

### 3. Установка зависимостей

Убедитесь, что у вас установлены основные пакеты:
Bash
```
sudo pacman -S hyprland kitty fish tmux vim stow fastfetch rofi waybar mako yazi
```
#### 📝 Заметки по использованию
    Vim: Не забудьте запустить :PlugInstall (или команду вашего менеджера плагинов) при первом запуске Vim, чтобы подтянуть все зависимости в ~/.vim/plugged.

    Tmux: Для установки плагинов нажмите Prefix + I (по умолчанию Ctrl+B, затем Shift+I), если используете TPM (Tmux Plugin Manager).

    Fish: Чтобы сделать fish оболочкой по умолчанию, выполните: chsh -s $(which fish).
